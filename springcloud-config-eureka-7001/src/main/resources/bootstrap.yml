#bootstrap.yml系统级别的配置
spring:
  cloud:
    config:
      #需要从git上读取的配置文件的名称,不需要后缀
      name: config-eureka
      #获取上述文件中的dev的环境
      profile: dev
      #分支
      label: master
      #config的服务端地址
      uri: http://localhost:8101